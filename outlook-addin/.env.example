# API Base URL
# For Railway production: https://your-backend-service.railway.app
# For local development: leave empty (uses relative URLs)
VITE_API_URL=

# Debug Configuration
# Set to 'true' to enable detailed API client debugging logs (for troubleshooting authentication issues)
# Logs include: CSRF token lifecycle, API request/response status, session cookie presence
# WARNING: Only enable temporarily for debugging - disable in production to avoid console spam
# Performance impact: None (logs only, doesn't affect API calls)
VITE_DEBUG_API=false
